// Alpine.js is now provided by Livewire's own bundle (loaded via @livewireScripts
// in the layout), so it is intentionally not imported/started here to avoid a
// double-initialization conflict between two separate Alpine instances.
